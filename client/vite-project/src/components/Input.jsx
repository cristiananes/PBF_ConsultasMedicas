export const Input = ({ text }) => {
    return (
        <Input className="border border-gray-300 rounded-md px-4 py-1 mt-1 focus:outline-none focus:ring-2 focus:ring-teal-500">
            {' '}
            {text}
        </Input>
    );
};
